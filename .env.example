# Supabase Configuration (Required for all environments)
VITE_SUPABASE_URL=your-project-url-here
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Backend Configuration (Only for local development)
BACKEND_PORT=3001

# Production Deployment Notes:
# When deploying to Vercel, you only need to set:
# - VITE_SUPABASE_URL
# - VITE_SUPABASE_ANON_KEY
# These should be added in the Vercel dashboard under Settings > Environment Variables
